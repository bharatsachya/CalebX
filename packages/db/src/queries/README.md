# Candidate SQL Queries

This folder contains raw SQL query strings for the `candidates` table and related database operations.

## Contract

- Export plain parameterized SQL query strings (`$1`, `$2`, etc.).
- Do not execute database queries inside this folder — query execution is handled by repository files in `packages/db/src/`.
